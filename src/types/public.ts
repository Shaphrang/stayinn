export type PublicApiSuccess<T> = { success: true; data: T };
export type PublicApiError = { success: false; error: { code: string; message: string } };
export type PublicCategory = { label: string; value: string; count?: number };
export type PublicBanner = { id: string; title: string; subtitle: string | null; imageUrl: string; buttonLabel: string | null; linkType: string; linkValue: string | null };
export type PublicLocationOption = { id: string; name: string; slug: string; districtName?: string; stateName?: string; count?: number };
export type PublicStayCard = { id: string; slug: string; name: string; type: string; shortDescription: string | null; locality: string | null; district: string | null; addressLabel: string; priceFrom: number; rating: number; reviewCount: number; coverImageUrl: string; tags: string[]; isFeatured: boolean; isVerified: boolean; activeRoomCount: number; latitude: number | null; longitude: number | null };
export type PublicRoom = { id: string; propertyId: string; name: string; slug: string; roomType: string | null; description: string | null; maxAdults: number; maxChildren: number; maxGuests: number; weekdayRate: number; weekendRate: number; seasonRate: number | null; holidayRate: number | null; childRate: number | null; extraBedRate: number | null; allowExtraBed: boolean; amenities: string[]; coverImageUrl: string; galleryImageUrls: string[] };
export type PublicPropertyDetail = PublicStayCard & { description: string | null; address: string | null; landmark: string | null; pincode: string | null; contactPhone: string | null; whatsappNumber: string | null; galleryImageUrls: string[]; amenities: string[]; rules: string[]; checkInTime: string | null; checkOutTime: string | null; stateName: string | null; districtName: string | null; locationName: string | null };
export type PublicPropertyListResponse = { items: PublicStayCard[]; page: number; pageSize: number; total: number; hasMore: boolean };
export type PublicPropertyDetailResponse = { property: PublicPropertyDetail; rooms: PublicRoom[]; similarStays: PublicStayCard[] };
export type PublicFilterOptions = { propertyTypes: PublicCategory[]; states: Array<{ id: string; name: string }>; districts: Array<{ id: string; name: string; stateId?: string }>; locations: PublicLocationOption[]; amenities: Array<{ label: string; value: string; count?: number }>; priceRanges: Array<{ label: string; min: number; max: number | null }>; guestOptions: number[] };
export type PublicHomeData = { hero: { title: string; subtitle: string }; categories: PublicCategory[]; banners: PublicBanner[]; featuredStays: PublicStayCard[]; weekendGetaways: PublicStayCard[]; nearbyStays: PublicStayCard[]; popularLocations: PublicLocationOption[] };
export type PublicSearchParams = { q?: string; where?: string; checkIn?: string; checkOut?: string; guests?: number; propertyType?: string; locationSlug?: string; page?: number; pageSize?: number };
export type PublicSearchResponse = PublicPropertyListResponse & { appliedFilters: Record<string, unknown> };
export type PublicEventPayload = { eventType: string; propertyId?: string; bannerId?: string; category?: string; metadata?: Record<string, unknown> };
